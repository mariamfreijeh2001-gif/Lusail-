{
  "name": "lusail-corp-site",
  "version": "2.0.0",
  "private": true,
  "description": "Lusail Corp group website - static, generated from data/site.js",
  "scripts": {
    "build": "node build/render.js",
    "start": "node build/render.js && npx --yes serve dist -l 5173",
    "serve": "npx --yes serve dist -l 5173"
  }
}
